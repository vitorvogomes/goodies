// Placeholder protegido (gating via proxy.ts) — o dashboard real é do EPIC-07.
export default function DashboardPage() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center gap-2 text-center">
      <h1 className="font-mono text-2xl font-semibold text-foreground">Goodies</h1>
      <p className="text-foreground/50">Dashboard em construção.</p>
    </main>
  );
}
